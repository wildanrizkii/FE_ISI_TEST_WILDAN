import React from "react";
import Layout from "@/components/Layout";
import Task from "@/components/UI/Task";

const page = () => {
  return (
    <Layout>
      <Task />
    </Layout>
  );
};

export default page;
